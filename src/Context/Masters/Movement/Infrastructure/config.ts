import { database_logiflow } from '@Shared/Infrastructure/config'
import { collections } from 'logiflowerp-sdk'

export const database = database_logiflow
export const collection = collections.movements